# Changesets

This folder contains changesets for package releases. Run `pnpm changeset` to
create one; `pnpm version` applies pending changesets to `package.json` files;
`pnpm release` publishes to npm.
